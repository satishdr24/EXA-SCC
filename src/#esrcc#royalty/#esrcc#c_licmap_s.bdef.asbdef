projection;
strict;
extensible;
use draft;

define behavior for /ESRCC/C_LicMap_S alias LicenseeAll
extensible
{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action SelectCustomizingTransptReq;

  use association _Licensee { create; with draft; }
}

define behavior for /ESRCC/C_LicMap alias Licensee
extensible
{
  use update;
  use delete;

  use action Copy;

  use association _LicenseeAll { with draft; }
}
projection implementation in class /ESRCC/BP_C_ROYKEY_S unique;
strict;
extensible;
use draft;

define behavior for /ESRCC/C_RoyKey_S alias RoyKeyAll
extensible
{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action SelectCustomizingTransptReq;

  use association _RoyaltyKey { create ( augment ); with draft; }
}

define behavior for /ESRCC/C_RoyKey alias RoyaltyKey
extensible
{
  field ( modify )
  Description;

  use update ( augment );
  use delete;

  use action Copy;
  use action Submit;
  use action Finalize;
  use action Reopen;

  use association _RoyKeyAll { with draft; }
  use association _RoyaltyKeyText { create; with draft; }
}

define behavior for /ESRCC/C_RoyKeyText alias RoyaltyKeyText
extensible
{
  use update;
  use delete;

  use association _RoyKeyAll { with draft; }
  use association _RoyaltyKey { with draft; }
}
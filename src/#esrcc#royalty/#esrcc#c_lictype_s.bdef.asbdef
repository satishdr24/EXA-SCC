projection implementation in class /ESRCC/BP_C_LICTYPE_S unique;
strict;
extensible;
use draft;

define behavior for /ESRCC/C_LicType_S alias LicenseTypeAll
extensible
{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action SelectCustomizingTransptReq;

  use association _LicenseType { create ( augment ); with draft; }
}

define behavior for /ESRCC/C_LicType alias LicenseType
extensible
{
  field ( modify )
   Description;


  use update( augment );
  use delete;

  use action Copy;

  use association _LicenseTypeAll { with draft; }
  use association _LicenseTypeText { create; with draft; }
}

define behavior for /ESRCC/C_LicTypeText alias LicenseTypeText
extensible
{
  use update;
  use delete;

  use association _LicenseTypeAll { with draft; }
  use association _LicenseType { with draft; }
}
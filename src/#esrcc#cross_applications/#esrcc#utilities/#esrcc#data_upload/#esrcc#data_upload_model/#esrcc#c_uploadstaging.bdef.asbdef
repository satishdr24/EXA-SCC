projection;
strict ( 1 );
use draft;
extensible;
use side effects;

define behavior for /ESRCC/C_UploadStaging alias UploadStaging
extensible
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}